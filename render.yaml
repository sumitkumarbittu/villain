services:
  - type: web
    name: villain-api
    env: node
    region: oregon
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: villain-db
          property: connectionString
    healthCheckPath: /health
    autoDeploy: true

databases:
  - name: villain-db
    plan: free
    region: oregon
    databaseName: villain
    user: villain_user